{
  "name": "ffid-banner-qa-tools",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "worker"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:worker": "npm run dev -w worker",
    "setup:worker": "npm run install:browsers -w worker",
    "setup:cdnops-auth": "npm run setup:cdnops-auth -w worker",
    "build": "npm run build -w client && npm run build -w server",
    "build:render": "npm install --include=dev && npm run build",
    "start": "npm run start -w server",
    "test": "npm run test -w server && npm run test -w client && npm run test -w worker"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
